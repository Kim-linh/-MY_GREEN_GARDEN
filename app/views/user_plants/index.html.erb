<div class="container">

  <p>ceci est mon index</p>
  <% @user_plants.each do |user_plant| %>
    <%= user_plant.name %>
    <%= link_to "Acceder à la show", user_plant_path(user_plant) %>
  <% end %>

</div>
